node_modules/
dist/
*.tsbuildinfo
.env
.env.*
.openclaude-profile.json
reports/

# Graphify code-graph output (local dev tool, ~MB) — rebuilt via `graphify update .`, never commit.
graphify-out/
.graphifyignore
